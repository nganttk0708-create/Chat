extends ../../layouts/socialDashboard.pug

block main
    .friends-container
        h1.title Danh sách bạn bè

        .users-list(data-users-friend=user.id)
            each user in users
                .box-user(user-id=user.id)
                    .inner-avatar
                        img(src=user.avatar alt=user.fullName)
                    .inner-info
                        .inner-name= user.fullName
                        .inner-buttons
<<<<<<< HEAD
                            a.btn.btn-sm.btn-primary.mr-1(href=`/${user.friendInfo.room_chat_id}` btn-accept-friend=user.id) Nhắn tin
                    .inner-status(status=user.statusOnline)
=======
                            a.btn.btn-sm.btn-primary.mr-1(href=`/${u.friendInfo.room_chat_id}`) Nhắn tin
                            button.btn.btn-sm.btn-danger.btn-unfriend(data-user-id=u.id) Hủy kết bạn

                    .inner-status(status=u.statusOnline)
>>>>>>> e2bc948 (thong bao khi login logout)
                        i.fa-solid.fa-circle
